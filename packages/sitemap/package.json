{
	"name": "@crawl-me-maybe/sitemap",
	"version": "0.0.10",
	"type": "module",
	"sideEffects": false,
	"author": "Nathan Nye <nathan@nye.dev> (https://nye.dev)",
	"license": "MIT",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		},
		"./vite": {
			"types": "./dist/vite.d.ts",
			"import": "./dist/vite.js"
		}
	},
	"files": [
		"dist",
		"docs"
	],
	"scripts": {
		"build": "rm -rf dist && bun build src/index.ts --outdir dist --format esm --sourcemap && bun build src/vite.ts --outdir dist --format esm --sourcemap --target=node --external node:fs --external node:path && tsc -p tsconfig.json --declaration --emitDeclarationOnly --declarationDir dist --rootDir src"
	},
	"publishConfig": {
		"access": "public"
	},
	"devDependencies": {
		"@types/node": "^22.10.1"
	}
}
