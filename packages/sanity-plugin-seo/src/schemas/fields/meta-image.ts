import { defineField } from "sanity";

export default defineField({
	name: "metaImage",
	title: "Meta Image",
	type: "image",
	description:
		"An image displayed on social media platforms when the site is linked",
});
