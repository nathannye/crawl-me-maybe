{
  "name": "@crawl-me-maybe/web",
  "version": "0.0.7",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "lint": "biome check .",
    "fix": "biome check --write .",
    "build": "rm -rf dist && bun build src/index.ts --outfile dist/index.js --format esm --sourcemap --external @sanity/client --external @sanity-image/url-builder --external @sanity/image-url && tsc -p tsconfig.json --declaration --emitDeclarationOnly --declarationDir dist --rootDir src"
  },
  "dependencies": {
    "@sanity-image/url-builder": "^1.0.0",
    "@sanity/client": "^6.0.0",
    "@sanity/image-url": "^1.2.0"
  },
  "peerDependencies": {
    "@sanity/client": "^6.0.0"
  },
  "devDependencies": {}
}
